
version = "fixture"

[[instructions]]
family = "fixture"
variant = "from"
rust_name = "from"
emit = ["from", "$out", "$input"]
receiver = ""
inputs = ["input"]
outputs = ["out"]

[[instructions]]
family = "fixture"
variant = "into"
rust_name = "into"
emit = ["into", "$out", "$input"]
receiver = ""
inputs = ["input"]
outputs = ["out"]
